[phases.setup]
nixPkgs = ["php84", "php84Packages.composer"]

[phases.build]
cmds = ["composer install --no-dev --optimize-autoloader --no-scripts --no-interaction --ignore-platform-reqs"]

[start]
cmd = "php artisan package:discover --ansi && php artisan serve --host=0.0.0.0 --port=$PORT"